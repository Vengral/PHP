{% include 'default/_form.html.twig' with {
    page_title: 'title.payment_edit'|trans({'%id%': payment.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'payment_index',
} %}