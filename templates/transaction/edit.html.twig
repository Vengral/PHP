{% include 'default/_form.html.twig' with {
    page_title: 'title.transaction_edit'|trans({'%id%': transaction.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'transaction_index',
} %}